---
id: "2026-08-04-defs-us-pr"
status: in_progress
current_role: planner
branch: claude/defs-us-pr
worktree: /Users/nerya/LexGraph-wt/defs-us-pr
locked_by: null
locked_at: "2026-08-04T01:00:00Z"
last_agent: "claude-code:qa-cycle14-m-r18-m-r19-verification"
lint: "PASS 397 2026-08-05T18:59:11Z"
last_updated: "2026-08-05T18:59:11Z"
program: "2026-08-04-definition-completeness"
evaluator: custom
evaluator_command: "backend/.venv/bin/pytest backend/tests -v && npm --prefix frontend run test -- --run && npm --prefix frontend run typecheck"
total_items: 33
completed_items: 0
dev_complete_items: 14
qa_cycles: 4
previous_sprint: "2026-08-02-us-state-law"
prd_sections: []
design_sections:
  - docs/sprint/programs/2026-08-04-definition-completeness.md
  - docs/sprint/programs/2026-08-04-definition-completeness-recon.md
---

# Sprint: Puerto Rico — Spanish-language definition rules

## Mandate

Recon finding (dossier §6, B3b): the US-PR corpus (23,636 rows) is in
SPANISH and was 100% invisible to the English-only USProfile — 633 real
`Definiciones` headings undetected. This sprint builds PR's Spanish rule
set (headings, definition idioms, entry markers, scope phrases, citation
grammar) as `USProfile`-hosted rule modules behind the core seam. The
recon's sampled idiom counts were a LEAD; the measured survey is archived
in the log. Example row: `STATE_PR_LEY_249_2003_ART3`.

## Acceptance gates (program manager-defined)

- **P1 — Real PR statutes parse**: `Definiciones` sections are detected and
  their terms extracted, RED tests from real PR rows before implementation.
- **P2 — Spanish definition idioms are captured** (`significa`,
  `se entenderá por`, quoted-term appositions as found in the corpus),
  including definitions outside canonical Definiciones sections.
- **P3 — Scope stamped/enforced** for Spanish scope phrases
  (`A los fines de este Artículo…` → article scope, etc.) via the core
  seam, live-path both directions.
- **P4 — Zero-miss sweep (director bar)**: QA sweeps the full PR file for
  Spanish definition signals; every hit captured or proven
  not-a-definition. Report the before/after capture rate (before = 0).
- **P5 — Nothing regresses**: English-state behavior untouched (PR rules
  must not fire on English text); all existing tests green; P-R2 on
  precision conflicts.

## Coordination

SETTLED. The seam question resolved against the `PRProfile` proposal: PR
ships as `USProfile`-hosted rule modules (core M7), split per M-R13. The
idiom survey was run and is archived in the log. Core AND the dispatch
sprint are both MERGED (`8524067`) — all 8 rule kinds verified live, P-R8
closed. Out-of-family misses still route via the program manager.

## Standing constraints

All program standing constraints apply (program doc): CodeGraph first;
red-before-green live-path tests; Planner owns tests; QA independent; no
test downloads the corpus; absolute zero-miss bar; P-R2.

## Manager rulings (sprint manager, panel arbitration)

Panel dialogue is recorded in `2026-08-04-defs-us-pr-log.md` (append-only).

- **M-R1 — Workspace.** All work happens in worktree
  `/Users/nerya/LexGraph-wt/defs-us-pr` with its OWN backend venv
  (`backend/.venv`, python3.13, built 2026-08-04). No panel agent touches
  `/Users/nerya/LexGraph` except read-only `codegraph explore` (the
  `.codegraph/` index lives there and is built at `origin/main`, this
  worktree's base). Never `git stash`; push only `claude/defs-us-pr`.
- **M-R2 — Idiom inventory is a planning deliverable, not an input.** The
  recon's Spanish idiom list (`significa` 1,006 / `A los fines de` 261 /
  `se entenderá por` 26 in a 4,000-row sample) came from English-translated
  keyword guessing and is a LEAD. The Planner must run its own
  corpus-driven survey over the real `us_pr_statutes.parquet` and publish the
  measured inventory in this contract BEFORE item scope freezes. Gate P4's
  zero-miss sweep is only meaningful against a survey-grounded signal list.
- **M-R3 — Seam question is decided with core, not unilaterally.** Whether PR
  becomes a distinct profile class or a rule-set under `USProfile` is a
  cross-sprint seam decision. The Planner writes a proposal with reasons; the
  manager relays it to the core panel; disagreement escalates to the program
  manager. Until core publishes `## Seam spec`, developers implement only
  non-shared-module work.
- **M-R5 — Developer proceeds on seam-agnostic work while the seam proposal
  is under core review.** The Spanish rule logic (items 1/2/3-functions/4/6)
  is identical under both seam options, lives in the NEW module
  `pr_profile.py`, and touches no shared file — it is built now. Only
  REGISTRATION differs between the options and is a shared-module edit
  M-R3 already defers; items 5/7/8 stay parked until core publishes.
  Residual risk if core rejects the distinct-class proposal: the `PRProfile`
  wrapper and the tests' import surface get rehomed; the rule functions and
  every Spanish behavioral assertion survive. Deliberate trade, recorded.
- **M-R4 — P5 is a two-sided gate.** "PR rules must never fire on English
  text" needs a test that would FAIL if the Spanish rules were made
  language-blind — i.e. a real English-state row fed through the PR rule path
  proving no extra capture, not merely "English tests still pass".

## Spanish idiom survey (measured)

Full measured survey (cycle-1 baseline plus the cycle-2 and cycle-3
corrections) archived in the panel log under
`### Archived from contract — Spanish idiom survey`.

## Bucket D final split (cycle 3, DO NOT REOPEN — M-R10/cycle-4 brief)

**70 heading-anchored, 8 anchor-less residue.** Full method/derivation
and the 8-row by-`act_id` table archived verbatim in the panel log (moved
by the cycle-9 Planner to free contract budget — the `## Residual ledger`
entry below already points here; no content change, not a reopening).
Pinned in `test_pr_profile_bucket_d_heading_anchored.py` (7 rows) and
`test_pr_profile_cycle4_marker_gate_and_residue.py` (8th row).

## Archived to the panel log (pointers only — all settled)

- **Seam proposal** (D2/M-R3) — routed to core; settled against the
  `PRProfile` class per core M7 / M-R13. Log: `### Archived from contract —
  Seam proposal`.
- **Core seam coordination (D3)** — core and dispatch both MERGED; superseded.
- **Cycle-1 items 1-9**, **cycle-2 items 10-12**, **cycle-3 items 13-17** —
  all dev-complete or superseded; full plans in the log's per-cycle entries.

## Cycle-4 item plan (Planner, 2026-08-04 — items 18-25)

All items now dev-complete, superseded, or HELD (tracked live in `##
ESCALATION — RULED` and `## Dev Complete`). Full item descriptions,
P-R7-compliant sweep method, and the item-18c ship-it-vs-narrower data
archived verbatim in the panel log under `### Archived from contract —
cycle-4 item plan`.

## ESCALATION — RULED (program ruling P-R8, 2026-08-04, main @ 0f4e8fc)

Our finding — 5 of 7 registry rule kinds have no consumer in
`backend/app/` — upheld as **Option A**: core reopens
(`2026-08-04-defs-core-dispatch`) for all five dead kinds +
`determine_scope`'s missing rule seam + per-kind dispatch tests. Option C
(mis-scope via `ScopeTriggerRule`) rejected on our own reasoning; the
headings panel independently converged on the same finding. Dead-kind
items (19-24) and P3's chapter half stay HELD until core's dispatch
merges. Full evidence in the log's phase-2 entry.

## Cycle-5 item plan (Planner, 2026-08-04 — numbering continues from 25)

Items 26-29 (18a live seam split by M-R13 into 2 `ScopeTriggerRule`s;
`CitationRule`; P3 article-scope both directions; P5 two-sided
regression). All dev-complete AND QA cycle-5 verified (gate table below).
Full item descriptions, the M-R12 measurement, and the retargeted-test
reasoning are archived in the panel log's cycle-5 Planner entry and the
M-R13 ruling below.

## Cycle-7 item plan (Planner, 2026-08-04 — item 30, M-R14 gate 1 / M-R15 step 1)

30. **`_UNQUOTED_TERM_DASH_RE` precision fix — SETTLED, DONE.** Full item
    text archived in the panel log (pure de-duplication — `## M-R14`/
    `## Residual ledger` already carry forward everything still open).
    One-line summary: narrowed (not dropped) the term-capture group to
    refuse a `.-` boundary crossing, mirroring `_UNQUOTED_TERM_PERIOD_RE`'s
    own discipline; split settled at 44 rejected / 191 retained (M-R16);
    the hyphenated-prose-no-period residual stays open on the ledger.

## M-R13 — Option D ruled: item 18c DEFERS; item 26 splits (2 rules, not 3)
(fully discharged — compressed, full text/test-partition in the log)

Program manager ruled **Option D**: 18c waits for core's dispatch, whereupon
canonical rows route to `pipeline.py`'s `if` branch and never reach
`extract_local_scope_definitions` — the 38-row residual disappears by
construction, zero recall cost, zero wrong assertions. Item 26 shipped
**two** `ScopeTriggerRule`s, not three: `extract_local_definitions`
(widened, 18a) and `extract_adhoc_definitions` (unchanged);
`extract_inline_local_definitions` (18c) still not built (M-R14: unblocked,
future cycle). Standing duty carried into M-R14/M-R18: QA re-runs the
canonical-leak measurement against the dispatched pipeline empirically,
never assumes the by-construction claim.

## M-R12 — item 18c must ship the population it was measured on (DISCHARGED)

Full text archived in the panel log (P-R8/M-R12 entry, 2026-08-04). Its
standing re-measurement requirement is satisfied -- see `## M-R14` point 2
("SATISFIED... HOLDS").

## FINAL GATE TABLE (QA cycle-14, independent; supersedes cycle-11 on P3; full detail in log)

| Gate | Verdict | Basis |
|---|---|---|
| **P1** | **PASS** | Canonical dispatch (items 31-33) independently re-verified exact: 633/0 heading flip, 612/21 scope split, 102 zero-candidate rows, all reproduced by QA cycle-11 via `get_profile("US-PR")`. |
| **P2** | PARTIAL | Canonical half now live and correct (item 33). Non-canonical recall stays bounded by 18c's deliberate deferral (M-R13); the M-R18/M-R19 vocabulary widening improves recall within already-shipped rules, independently reproduced exact by QA cycle-14 (below). |
| **P3** | PARTIAL, PRIOR DEFECT CLOSED | Chapter half's QA-11-confirmed 6-row/42-candidate mis-scoping defect is FIXED and independently re-verified exact by QA-14 (21→28/633, 0 regressions, mutation-tested). Article half: 65 rows newly capture (exact match, mutation-tested); 161/246 structural residue remains (QA-14's own count; Developer's 162/247, 1-row gap in the already-ledgered residual denominator, explained, immaterial — see log). Título/Subtítulo/Subcapítulo gap routed to core-2/G6, unblocked, not yet built. |
| **P4** | HELD | Zero-miss bar still gated on items 19-24 (HELD, own future cycle — QA-11's 102-zero-candidate sweep found nothing beyond the already-catalogued population) and 18c (now safely deferrable per gate 2, not yet built). |
| **P5** | **PASS** | Re-confirmed a THIRD time by QA cycle-14 via a DIFFERENT method again (exact-match dispatch-filter code reading + live 54-jurisdiction-code registry sweep + the existing 12-test live DB-backed P5 suite), not merely re-running cycle-11's probes. |

M-R14 gate 2 (the 18c by-construction re-measurement) is SATISFIED — see
`## M-R14` above. QA cycle-11's full derivation, the new 5th-shape
escalation, and the 102-row zero-candidate characterization (36-row
sample) are in the panel log.

**Also settled:** 6/6 fixtures byte-exact (re-verified by the manager, not
taken from QA); of the 6 declared-vacuous greens, 5 became genuinely
meaningful post-registration and 2 remain vacuous for named structural
reasons (mutation-proven); counter audit closed with caveat (below).

## Residual ledger (named, open, NOT silently carried)

- **Hyphenated-prose FP class, no period** — `STATE_PR_LEY_163_2005_ART2`
  ("Consejo Juanadino Pro - Festejos"). Survives the gate-1 `.-` narrowing;
  pinned as a documented xfail, not hidden. The broader marker-lookahead
  narrowing that would catch it was measured to bleed into the
  typographic-dash branch's existing behavior and was rejected — follow-up
  option only. **Stays on this ledger until closed or explicitly accepted.**
- **8-row bucket-D anchor-less residue** (cycle 3) — documented by `act_id`
  in the log, correct-zero, per director ruling D-PR-A.
- **19 rows: `Título`/`Subtítulo`/`Subcapítulo` scope kinds** (2/11/6,
  canonical first sentences, same grammatical family as `Capítulo`, genuine on
  hand-read). **Closing condition: post-core-2-merge `ScopeKindRule` build** —
  kinds `title`/`subtitle`/`subchapter`, values via `detect_value`. Core-2's
  **G6** shipped exactly this mechanism (`ScopeKindRule.detect_value`,
  `determine_scope_assignments` replaying dispatch with rule-supplied kind
  STRINGS under M-D2 first-non-None-wins, matcher containment generic over
  kinds), so this is **family-buildable with zero core edits** once core-2
  merges — our merge slot already follows it. Share design with the
  **headings panel's VA `title` build**, recorded in G6's 8-row follow-on
  table (same wall, same solution).
- **Reading-substitution idiom FP** — `STATE_PR_RENTAS_SEC1102_01`: `el
  término "X" según se usa en la Sección Y deberá leerse "Z"` shares the
  new `"el término "` lead-in's surface shape but is a cross-reference
  re-interpretation, not a definition. 1/80 hand-checked (98.75% genuine).
  Not patched: disambiguation is new precision machinery, out of scope for a
  vocabulary-closure pass.
- **161/246-row article-scope structural residue** (M-R19(3); QA-14's own
  independently re-derived count — Developer's 162/247 differs by 1 row,
  confined entirely to this residual denominator, explained and immaterial
  in the log). Multi-term colon-list shape the single-clause regexes cannot
  represent; a splitting design decision adjacent to items 19-24, not a
  vocabulary tweak.
- **233-vs-235 changed-outcome total** (M-R16, open). QA cycle-8's
  from-scratch splitter measured 233 where three earlier scripts measured
  235. **Not settled by majority:** QA cycle-8 alone documents a full-corpus
  zero-mismatch fidelity proof against production's real
  `extract_definitions_from_section` (23,636 rows × 2 regex states), so 235
  may be 2 too high. Immaterial to every decision made — `reject = 44` is
  invariant across all five derivations, the gap is confined to the
  "brand-new" sub-count (211 vs 213, "reclassified" 22 = 22), and both
  genuine anchor rows survive under every measurement. Whoever reopens it
  should start from QA cycle-8's method, not the majority.
- **Baseline block-splitter collision, 21/633 canonical rows** (cycle-9,
  ESCALATED — see `## Cycle-9 item plan`). Shared `us_profile.
  _split_into_numbered_blocks`/`_leading_quote_candidate` fabricates a
  wrong (not just incomplete) `definition_text` for one term per
  affected row once the `HeadingRule` (item 31) is registered. Pinned
  `xfail(strict=False)`, QA cycle-11 independently reproduced 21/633
  EXACT and re-confirmed still XFAIL. **Stays on this ledger until the
  manager picks an option and it is closed or explicitly accepted.**
- **M-R18 closed chapter/article vocabulary + chapter-scope anchoring gap —
  CLOSED, QA-14 VERIFIED.** Fifth chapter-scope shape (6/633, no "los"),
  the anchoring gap (1 row, `STATE_PR_LEY_77_1957_ART23_010`, trigger in
  sentence 2 behind a short-title clause), and the article-scope widening
  (Sección support, "los"-optional, "el término" lead-in) all shipped
  `cc1cf68`. QA cycle-14 independently re-derived: chapter 21→28/633 exact,
  article-scope +65 exact, both zero regressions, all 25 target assertions
  mutation-killed precisely (log has full derivation). Reading-substitution
  FP (`STATE_PR_RENTAS_SEC1102_01`, 1/80) and the 161/246 structural
  residue (above) are the only open remainders.
- **ESCALATED (QA-14) — a second text-fidelity class in the 65 newly-closed
  rows' `definition_text`, both PRE-EXISTING and corpus-wide, not
  introduced by this cycle.** (a) An unhandled second page-break footer
  family, `"LexJuris de Puerto Rico ©2011 www.LexJuris.net <N>"` —
  `_PAGE_BREAK_FOOTER_RE` only recognizes the `ogp.pr.gov` shape. Leaks
  mid-span into 6/80 of this cycle's candidates; corpus-wide, 453 rows
  contain it, 0/453 stripped, 12 are already-shipped CANONICAL rows, and
  5 of those 12 already have it corrupting a LIVE `definition_text` today.
  (b) A third instance of the "abbreviation/list-marker period fools the
  non-greedy capture boundary" bug class (cycle-6's "Rev.", M-R19(1)'s own
  "[Nota:...] Art." accident): `STATE_PR_LEY_249_2008_ART5`'s 3-item
  enumerated definition captures as just `"significará: i."`. Neither is a
  classification false-positive (genuineness stays 79/80 = 98.75%); both
  are invisible to an opening-verb genuineness check by construction. Not
  fixed (QA write-set). **Routed to the manager/director — full counts and
  the 3 leading-comma cosmetic cases in the log.**
- **ESCALATED (QA-14) — the M-R19(1) anchoring gate's exposure surface: 13
  gate-fires for 1 genuine case, 0 live consequences today.** Adversarially
  probed both directions. Too broad: `_SHORT_TITLE_NAMING_CLAUSE_RE` fires
  on 13/633 canonical rows but only 1 (the intended row) also has a
  sentence-2 trigger; the other 12 fire on cross-references to a
  DIFFERENT law's short title (7-9 rows — a common PR drafting idiom), a
  word-boundary bug ("reconocida" containing "conocida como", no leading
  `\b`), and unrelated alternate-name glosses — safe today only because
  sentence 2 doesn't happen to also match, a corpus-composition coincidence
  not a structural guarantee. Too narrow: checked; not — the 5 rows with a
  genuine-looking sentence-2 trigger the gate doesn't reach are all
  correctly-excluded (tautological self-reference / narrower-than-chapter
  scope / already-named ungated-draft false positives). **Not fixed
  (QA write-set) — routed to the manager/director, full row-by-row
  disposition in the log.**
- **Título/Subtítulo/Subcapítulo scope-kind gap — degeneracy premise
  SETTLED (QA-14): CONFIRMED, recorded as a named characterization per the
  pre-agreed framing, not a defect.** Re-grouped by the parquet's own
  `title_name` (actual law/code identity — the manager's own 3-token-prefix
  spot-check was too coarse to test this). All 6 law codes contributing to
  the NOW-28-row chapter-scoped population (was 21) have exactly 1 distinct
  `chapter` value across their own entire corpus-wide row population
  (Código de Seguros 1015 rows, Rentas Internas 783, Incentivos 250, Ley de
  la Policía 91, Comisión de Juegos 101, Autoridad Metropolitana de
  Autobuses 24 — all **1** distinct value each); Código Civil (1,821 rows)
  has **7** and is confirmed not among the 28. Verified at the MECHANISM
  level too, not just the data level: `matcher.py`'s `_in_scope` compares
  `article.chapter == definition.source_chapter` directly, which this data
  shape makes structurally equivalent to `"law-wide"`'s unconditional
  `True`. Does not gate the close. The scope-kind gap itself (2/11/6 rows,
  no home in the 2-way contract) stays **routed to the director** per
  D-Q1, unaffected by this settlement — full per-code table in the log.

## M-R14 — ENTRY CRITERIA for the held work — ALL 3 DISCHARGED

Gated item 18c/19-24 resumption on 3 points, all now resolved (full text
in the log): (1) `_UNQUOTED_TERM_DASH_RE` narrowed, cycle-7 item 30,
DONE. (2) 18c's by-construction claim re-measured empirically against the
dispatched pipeline — **SATISFIED** (QA cycle-11: 100% of the 633-row
canonical superset dispatches away from `extract_local_scope_definitions`;
a real 21-row `run_definition_linking` sample produced 442 Definitions, 0
`scope="local"`). Item 18c unblocked for a future cycle. (3)
`extract_local_definitions`'s false "positions unchanged" docstring —
corrected (`pr_profile.py:982` now states positions shift).

**Counter audit (closed, with caveat):** QA reconstructed the inherited
`dev_complete_items: 10` as items 1,2,4,10-16 — arithmetic-consistent but
NOT directly recorded. Provenance unrecoverable; `14` stands. Closed.

## Cycle-9 item plan (Planner, 2026-08-05 — M-R15 step 2, items 31-33) — QA CYCLE-11 VERIFIED

31-33 (Spanish `HeadingRule`/`ScopeKindRule`/`EntrySplitterRule`+
`TermClauseRule`, gate P1) shipped `35f5f6f`. QA cycle-11 independently
reproduced the headline numbers EXACT (633/0 heading flip, 612/21 scope
split, 102 zero-candidate rows, the 21-row baseline-collision defect
population) via `get_profile("US-PR")` and a real DB-backed
`run_definition_linking` sample — full derivation, the 3-candidate
measurement-fidelity note, and the 102-row characterization (36-row
sample, not 3) in the panel log's QA cycle-11 entry. Full original item
text also archived there.

### ESCALATION — pre-existing baseline defect exposed by item 31 alone (OPEN)

Shared, core-owned `us_profile._split_into_numbered_blocks` +
`_leading_quote_candidate` misfires on 21/633 canonical rows, fabricating
a wrong `definition_text` for one term per row — independently
reproduced EXACT by QA cycle-11. No family-panel-writable fix exists.
Pinned `xfail(strict=False)`, still XFAIL (QA-11 re-confirmed). Options
+ lean in the log's cycle-9 Planner entry — routed, not decided here.
Tracked on the Residual ledger below; stays open until the manager rules.

### ESCALATION — fifth chapter-scope shape (QA cycle-11) — SUPERSEDED

Superseded by the M-R18 closed-vocabulary work — see `## Residual ledger`'s
"M-R18 closed chapter/article vocabulary" entry and the log's M-R18
Planner (cycle-12) entry for the full closure (this shape plus the larger
article-scope gap it predicted the class of).

## Dev Complete

- **18a + 26** — 2 `ScopeTriggerRule`s registered (`us_pr_scope_triggers.py`);
  `extract_local_definitions` widened (QA cycle-4 findings 1-4; finding 5
  correctly left strict-xfail). Manager-verified `0ed731e`.
- **27** — `CitationRule` registered (`us_pr_citations.py`), L.P.R.A.
  baseline-ordering limitation pinned as accepted.
- **28** — P3 article-scope, both directions, live-path.
- **29** — P5 two-sided language regression, live-path.

## M-R17 — 6 unsatisfiable scope REDs RETIRED (Planner cycle-12)

`test_pr_profile_scope_cycle4.py`'s 6 REDs (`PRProfile.determine_scope`,
an interface that never existed and never will under the settled seam) are
now `pytest.mark.skip` — not deleted, not left permanently red. Full
rationale, the per-test mapping to their live cycle-9 replacements, and why
skip (not xfail, not deletion) is in the file's own rewritten docstring and
the log's M-R17 Planner entry. Suite: 6 fewer failures, 6 more skips, 0
coverage lost.

## Completed

_None._

## Context Dump

**QA cycle-14 done (independent):** re-derived M-R18/M-R19 (`cc1cf68`)
EXACT (21→28/633 chapter, +65 article, 0 regressions, 25/25 mutation-killed);
genuineness 79/80=98.75%, matches Developer. Degeneracy premise, properly
grouped by law code: **CONFIRMED** (6 codes, 1 distinct `chapter` value
each; Código Civil 7, excluded) — named characterization, not a defect, does
not gate. 2 NEW escalations (both pre-existing, not this cycle's fault): a
2nd unhandled footer family (LexJuris.net, already live in 5 canonical
rows) and the anchoring gate's 13-fires-for-1-genuine exposure surface.
Suite `24F/1034P/6skip/13x`. **NEXT: manager rules on the 2 escalations.**
