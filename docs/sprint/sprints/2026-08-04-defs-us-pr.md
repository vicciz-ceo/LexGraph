---
id: "2026-08-04-defs-us-pr"
status: in_progress
current_role: developer
branch: claude/defs-us-pr
worktree: /Users/nerya/LexGraph-wt/defs-us-pr
locked_by: null
locked_at: "2026-08-04T01:00:00Z"
last_agent: "claude-code:sprint-manager"
lint: "PASS 397 2026-08-04T15:51:43Z"
last_updated: "2026-08-04T15:51:43Z"
program: "2026-08-04-definition-completeness"
evaluator: custom
evaluator_command: "backend/.venv/bin/pytest backend/tests -v && npm --prefix frontend run test -- --run && npm --prefix frontend run typecheck"
total_items: 29
completed_items: 0
dev_complete_items: 14
qa_cycles: 3
previous_sprint: "2026-08-02-us-state-law"
prd_sections: []
design_sections:
  - docs/sprint/programs/2026-08-04-definition-completeness.md
  - docs/sprint/programs/2026-08-04-definition-completeness-recon.md
---

# Sprint: Puerto Rico — Spanish-language definition rules

## Mandate

Recon finding (dossier §6, B3b): the US-PR corpus (23,636 rows) is in
SPANISH and was 100% invisible to the English-only USProfile — 633 real
`Definiciones` headings undetected. This sprint builds PR's Spanish rule
set (headings, definition idioms, entry markers, scope phrases, citation
grammar) as `USProfile`-hosted rule modules behind the core seam. The
recon's sampled idiom counts were a LEAD; the measured survey is archived
in the log. Example row: `STATE_PR_LEY_249_2003_ART3`.

## Acceptance gates (program manager-defined)

- **P1 — Real PR statutes parse**: `Definiciones` sections are detected and
  their terms extracted, RED tests from real PR rows before implementation.
- **P2 — Spanish definition idioms are captured** (`significa`,
  `se entenderá por`, quoted-term appositions as found in the corpus),
  including definitions outside canonical Definiciones sections.
- **P3 — Scope stamped/enforced** for Spanish scope phrases
  (`A los fines de este Artículo…` → article scope, etc.) via the core
  seam, live-path both directions.
- **P4 — Zero-miss sweep (director bar)**: QA sweeps the full PR file for
  Spanish definition signals; every hit captured or proven
  not-a-definition. Report the before/after capture rate (before = 0).
- **P5 — Nothing regresses**: English-state behavior untouched (PR rules
  must not fire on English text); all existing tests green; P-R2 on
  precision conflicts.

## Coordination

SETTLED. The seam question resolved against the `PRProfile` proposal: PR
ships as `USProfile`-hosted rule modules (core M7), split per M-R13. The
idiom survey was run and is archived in the log. Core is MERGED; the
remaining dependency is the **dispatch** sprint (see M-R14 entry criteria).
Out-of-family misses still route via the program manager.

## Standing constraints

All program standing constraints apply (program doc): CodeGraph first;
red-before-green live-path tests; Planner owns tests; QA independent; no
test downloads the corpus; absolute zero-miss bar; P-R2.

## Manager rulings (sprint manager, panel arbitration)

Panel dialogue is recorded in `2026-08-04-defs-us-pr-log.md` (append-only).

- **M-R1 — Workspace.** All work happens in worktree
  `/Users/nerya/LexGraph-wt/defs-us-pr` with its OWN backend venv
  (`backend/.venv`, python3.13, built 2026-08-04). No panel agent touches
  `/Users/nerya/LexGraph` except read-only `codegraph explore` (the
  `.codegraph/` index lives there and is built at `origin/main`, this
  worktree's base). Never `git stash`; push only `claude/defs-us-pr`.
- **M-R2 — Idiom inventory is a planning deliverable, not an input.** The
  recon's Spanish idiom list (`significa` 1,006 / `A los fines de` 261 /
  `se entenderá por` 26 in a 4,000-row sample) came from English-translated
  keyword guessing and is a LEAD. The Planner must run its own
  corpus-driven survey over the real `us_pr_statutes.parquet` and publish the
  measured inventory in this contract BEFORE item scope freezes. Gate P4's
  zero-miss sweep is only meaningful against a survey-grounded signal list.
- **M-R3 — Seam question is decided with core, not unilaterally.** Whether PR
  becomes a distinct profile class or a rule-set under `USProfile` is a
  cross-sprint seam decision. The Planner writes a proposal with reasons; the
  manager relays it to the core panel; disagreement escalates to the program
  manager. Until core publishes `## Seam spec`, developers implement only
  non-shared-module work.
- **M-R5 — Developer proceeds on seam-agnostic work while the seam proposal
  is under core review.** The Spanish rule logic (items 1/2/3-functions/4/6)
  is identical under both seam options, lives in the NEW module
  `pr_profile.py`, and touches no shared file — it is built now. Only
  REGISTRATION differs between the options and is a shared-module edit
  M-R3 already defers; items 5/7/8 stay parked until core publishes.
  Residual risk if core rejects the distinct-class proposal: the `PRProfile`
  wrapper and the tests' import surface get rehomed; the rule functions and
  every Spanish behavioral assertion survive. Deliberate trade, recorded.
- **M-R4 — P5 is a two-sided gate.** "PR rules must never fire on English
  text" needs a test that would FAIL if the Spanish rules were made
  language-blind — i.e. a real English-state row fed through the PR rule path
  proving no extra capture, not merely "English tests still pass".

## Spanish idiom survey (measured)

Full measured survey (cycle-1 baseline plus the cycle-2 and cycle-3
corrections) archived in the panel log under
`### Archived from contract — Spanish idiom survey`.

## Bucket D final split (cycle 3, DO NOT REOPEN — M-R10/cycle-4 brief)

Full method/derivation archived in the panel log under the cycle-3
Planner entry ("Bucket D final result"). **70 heading-anchored, 8
anchor-less residue** (cycle-3 shipped 7; QA cycle-4 found the 8th,
below — documentation-only addition, the rule itself is unchanged and
not reopened).

**Final documented residue (8 rows, by `act_id`)** — every row below
correctly yields NOTHING from the heading-anchored rule, each for an
independently-diagnosed reason:

| `act_id` | `section_title` | Why it stays residue |
|---|---|---|
| `STATE_PR_CIVIL_ART1526` | Enriquecimiento sin causa; definición | Nominalization mismatch — body only uses the VERB form ("se enriquece"), never the noun phrase. |
| `STATE_PR_LEY_77_1957_ART35_020` | Definición | Bare heading, names no term at all. |
| `STATE_PR_LEY_77_1957_ART42_010` | Organizaciones caritativas, definición | Term never repeated verbatim anywhere in an 897-char qualifying-description body. |
| `STATE_PR_CIVIL_ART1293` | Las normas de la compraventa; definición y aplicabilidad | Heading/body term MISMATCH — heading names "las normas de la compraventa", body actually defines "permuta". |
| `STATE_PR_LEY_77_1957_ART4_010` | Definiciones que no se excluyen | Meta-heading about definitions in general, names no specific term. |
| `STATE_PR_PENAL_ART15` | Definición | Second real bare-heading example (different law). |
| `STATE_PR_LEY_77_1957_ART5_030` | Activo no Admitido, definición | Non-contiguous term — body says "un activo es uno no admitido", never the contiguous phrase "Activo no Admitido". |
| `STATE_PR_LEY_77_1957_ART36_010` | Sociedades fraternales benéficas—Definiciones | Singular/plural inflection mismatch — heading's plural "Sociedades…benéficas" vs. body's singular "una sociedad fraternal benéfica" (QA cycle-4 finding, same character as the `ART1526` row). |

Pinned in `test_pr_profile_bucket_d_heading_anchored.py` (7 rows) and
`test_pr_profile_cycle4_marker_gate_and_residue.py` (8th row, new).

## Seam proposal (Planner recommendation, D2/M-R3 — cross-sprint, core Planner to review)

Archived in the panel log under `### Archived from contract — Seam
proposal`. Status: routed to core by the program manager; core's seam
spec v2 will rule. Build as rule modules meanwhile (manager M-R8).

## Core seam coordination status (D3)

Archived in the panel log. Core published its seam spec at
`origin/claude/defs-core-scope` @ `9272f6e` (baseline-first rule
registry). Core-gated items stay parked until core merges.

## Next Steps

Cycle-1 items 1-9 (all dev-complete or core-parked) archived in the
panel log under `### Archived from contract — cycle-1 item plan`.
Cycle-3 items 13-17 archived per the note above. Active plan is the
cycle-4 item plan below.

## Cycle-2 item plan (Planner, 2026-08-04 — numbering continues from item 9)

Items 10-12, all dev-complete. Archived in the panel log under
`### Archived from contract — cycle-2 item plan`.

## Cycle-3 item plan (Planner, 2026-08-04 — items 13-17)

All dev-complete/QA'd. Archived in the panel log under the cycle-3
Planner/Developer entries. Active plan is the cycle-4 item plan below.

## Cycle-4 item plan (Planner, 2026-08-04 — numbering continues from item 17)

**P-R7-compliant sweep method** (own scripts, scratchpad, never touching
`backend/tests`): built ground truth independently of the extractor's own
idiom list — (a) a comprehensive idiom census swept corpus-wide (not
heading-gated) with per-idiom sample classification (n=11-25/idiom,
manually read), and (b) STRUCTURAL sweeps independent of idiom vocabulary
entirely (blank-title bodies, bare-term headings). Full tables, sample
transcripts, and the false-positive-rate data behind every item below are
in the panel log's cycle-4 Planner entry — this section is the dense
summary. Items 18-20 are **core-gated** (target the not-yet-landed
`extract_local_scope_definitions`/`derive_heading_from_body` seam
methods, spec v2.4); items 21-24 are pre-core OK (pure `pr_profile.py`
module additions); item 25 converts the P3 xfails to real RED tests.

18. **LEAD — `extract_local_scope_definitions` for PR (gate P2, M-R11).**
    New `PRProfile` method unioning three sources, scope="local": (a)
    `extract_local_definitions` widened per QA's findings 1-2 (already
    RED in `test_pr_profile_qa_cycle4_findings.py` — `se define` lead-in,
    unquoted term, missing `A los efectos de este Artículo` trigger); (b)
    `extract_adhoc_definitions` unchanged; (c) NEW — a bounded (word-
    boundary, no-unbounded-search) whole-body `finditer` sweep reusing
    the existing per-block QUOTED separator patterns, catching a quoted-
    term+idiom definition ANYWHERE in a non-canonical article regardless
    of scope-trigger phrase (measured: 889 corpus-wide hits, ~92-96%
    genuine on two independent random samples — see ESCALATION below).
    Proven by `test_pr_profile_local_scope_definitions_cycle4.py`.
19. **Blank-title Definiciones sections (gates P2, P4).** 47 corpus-wide
    rows whose `section_title` is a bare `"Artículo N."`/`"Sección N."`
    (zero descriptive text) but whose body opens with a Definiciones-
    block preamble (`"Los siguientes términos/vocablos/palabras/frases…
    tendrá(n) el/los significado(s)…"`); +3 more where a Definiciones
    sub-heading is embedded inside an amendment-instruction article.
    Live-verified: `extract_definitions_from_section` ALREADY parses
    these bodies correctly once called directly (e.g.
    `STATE_PR_LEY_241_1950_ART2` → 6/6 correct terms) — this is a
    heading-recognition-only gap, needs `derive_heading_from_body`, not
    new extraction logic. `STATE_PR_LEY_36_1984_ART9` is a harder related
    case (unmarked repeated "Term. — Definition." entries, no `(a)/(1)`
    markers, plus mid-body footer text) flagged as an additional,
    distinct entry-splitter gap. Proven by
    `test_pr_profile_derived_heading_definitions_cycle4.py`.
20. **Bare-term-heading definitions — the TRANSITO class (gates P2, P4).**
    117 corpus-wide rows (116 from PR's Traffic Code, 1 elsewhere) whose
    heading IS the term itself (no "definición" stem) and whose body
    opens, after an optional `[citation bracket]`, with a quoted term +
    recognized idiom. NEW function `extract_bare_term_heading_definition`
    — narrow by construction (mirrors the bucket-D discipline: no general
    prose matcher). A broader, unquoted heading-anchor generalization was
    measured and REJECTED (1,702 corpus-wide hits, sampled overwhelmingly
    false — `"Artículo 1150. Orden"` → body about payment order, not a
    definition). Proven by `test_pr_profile_bare_term_heading_cycle4.py`.
21. **17 real ordinary misses among the 33 canonical zero-yield rows
    (QA root-cause groups A-N, gate P4).** 15 new fixture rows (2 of the
    17 already QA-vendored: `quiere decir`/`STATE_PR_LEY_82_1964_ART3`,
    ASCII-hyphen/`STATE_PR_LEY_209_2016_ART2`; 1 reused from cycle 2:
    `STATE_PR_LEY_15_1931_SEC22`). Proven by
    `test_pr_profile_ordinary_misses_cycle4.py`.
22. **Marker-gate over-suppression (M-R9 fold-in, gate P4).** QA's lean
    adopted as the Planner's own design: narrow
    `extract_heading_anchored_definition`'s precondition from "body has
    ANY marker" to "the CORROBORATING SENTENCE itself has no marker" —
    the M-R7 guard rows (conditions/subsection-labels) stay at zero under
    this narrower gate (independently re-verified); the new 4th
    supporting row (`STATE_PR_RENTAS_SEC2041_03`) must now yield 1.
23. **Residue table 8th row (documentation, gate P4).**
    `STATE_PR_LEY_77_1957_ART36_010` — see the updated `## Bucket D final
    split` table above. GREEN correct-zero guard (already yields `[]`).
24. **Scrape-footer artifact in `extract_definitions_from_section`'s
    shared scan path (gate P4/P5).** Live-CONFIRMED real bug, not
    hypothetical: `STATE_PR_LEY_240_2002_ART3` (real, vendored, NOT
    previously used in any test) currently produces a fabricated
    candidate whose term is a prose fragment and whose `definition_text`
    is literally the footer boilerplate (`"Rev. 15 de abril de 2024
    www.ogp.pr.gov Página 2 de 5…"`) — the footer-stripping cycle 3 added
    only inside `extract_heading_anchored_definition`, never in this
    function's own per-block scan. Proven by
    `test_pr_profile_footer_artifact_cycle4.py`.
25. **P3 xfail → real RED tests (gate P3, core-gated).**
    `test_pr_profile_scope.py`'s 6 xfails converted to genuine RED tests
    against seam v2.4's real method name, `PRProfile.determine_scope`
    (not the Planner's cycle-1 placeholder name
    `determine_chapter_scope`) — RED via `AttributeError` against the
    un-rebased tree (expected, per M-R11's sequencing note). Proven by
    `test_pr_profile_scope_cycle4.py` (new file; the old
    `test_pr_profile_scope.py` is left untouched, superseded not edited).

### Item 18c escalation — RULED

D-PR-18c ruled ship-it (Option A) with the re-mention guard; ruling
M-R12 (below) added the canonical-population guard requirement.
Superseded by the cycle-5 item plan's item 26. Ship-it-vs-narrower data
archived in the log's cycle-4 Planner entry.

## ESCALATION — RULED (program ruling P-R8, 2026-08-04, main @ 0f4e8fc)

Our finding — 5 of 7 registry rule kinds have no consumer in
`backend/app/` — upheld as **Option A**: core reopens
(`2026-08-04-defs-core-dispatch`) for all five dead kinds +
`determine_scope`'s missing rule seam + per-kind dispatch tests. Option C
(mis-scope via `ScopeTriggerRule`) rejected on our own reasoning; the
headings panel independently converged on the same finding. Dead-kind
items (19-24) and P3's chapter half stay HELD until core's dispatch
merges. Full evidence in the log's phase-2 entry.

## Cycle-5 item plan (Planner, 2026-08-04 — numbering continues from 25)

Items 26-29 (18a live seam split by M-R13 into 2 `ScopeTriggerRule`s;
`CitationRule`; P3 article-scope both directions; P5 two-sided
regression). All dev-complete AND QA cycle-5 verified (gate table below).
Full item descriptions, the M-R12 measurement, and the retargeted-test
reasoning are archived in the panel log's cycle-5 Planner entry and the
M-R13 ruling below.

## M-R13 — Option D ruled: item 18c DEFERS; item 26 splits (2 rules, not 3)

Program manager ruled **Option D** on the escalation below: 18c waits for
core's dispatch, whereupon canonical rows route to `pipeline.py`'s `if`
branch and never reach `extract_local_scope_definitions` — the 38-row
residual disappears by construction, at zero recall cost and zero wrong
assertions (vs. A = 467 wrong assertions on a falsified premise, B = 92.7%
recall lost, C = 6-31% recall lost with 11-40 wrong assertions remaining).

Item 26 therefore ships **two** `ScopeTriggerRule`s, not three:
`extract_local_definitions` (widened, 18a — explicit `A los fines/efectos de
este Artículo` triggers, genuinely local scope) and `extract_adhoc_
definitions` (unchanged). **`extract_inline_local_definitions` and the M-R12
guard are NOT built this cycle.**

Test partition (verified row-by-row against the fixtures). **GREEN target,
10 REDs:** `qa_cycle4_findings.py` (5), `TestExtractLocalScopeDefinitions
Seam::test_unions_the_local_trigger_extractor` (1), `scope_triggers_live_
pipeline_cycle5.py::test_run_definition_linking_captures_…` (1 — its row
`STATE_PR_LEY_85_2018_ART9_04` is an ad-hoc `conocida como` capture, not an
18c hit), `citation_rule_live_cycle5.py` (1), `article_scope_live_cycle5.py`
(2 — built on `A los fines de este Artículo`, an 18a trigger). **STAY RED
(18c deferred):** `TestExtractInlineLocalDefinitions` (3),
`test_unions_the_new_inline_scan` (1), `test_documented_residual_…` (1).
Expected after Developer: 35 failed / 910 passed.

The M-R12 guard work is retained as the standing proof of what 18c must
re-verify post-dispatch: **QA re-runs the canonical-leak measurement against
the dispatched pipeline to confirm the by-construction claim empirically —
never assume it.** Separately, `test_documented_residual_…` now pins
OVERRULED behavior (Option A) and must be deleted or inverted by the
**Planner** (not the Developer) in a re-partition pass before QA.

## M-R12 — item 18c must ship the population it was measured on (superseded in part by M-R13)

Full text archived in the panel log (P-R8/M-R12 entry, 2026-08-04). Standing
requirement that survives M-R13: 18c may only ever ship the population whose
precision was measured, and its post-dispatch by-construction claim must be
**re-measured empirically against the dispatched pipeline, never assumed**.

## FINAL GATE TABLE (QA cycles 5+6, independent; full detail in log)

| Gate | Verdict | Basis |
|---|---|---|
| **P1** | PARTIAL / HELD | Canonical `Definiciones` dispatch needs `HeadingRule`, which does not dispatch until core's dispatch sprint merges. Untouched by cycle 5-6 work. |
| **P2** | PARTIAL | Live capture **15/766 (1.96%)** against a signal-agnostic P-R7 denominator, up from 0%. The precision caveat is **RESOLVED** (stray-quote 9/32→0/32, footer-truncation 2/12→0/12, exhaustive re-sweep). Recall — i.e. deferred 18c — is now the ONLY thing holding P2 below PASS. |
| **P3** | PARTIAL | Article-scope half PASSES live-path both directions; chapter half needs `determine_scope`'s rule seam, which core's dispatch sprint owns. |
| **P4** | HELD | Zero-miss bar is gated on P1 + 18c. Cycle 5-6 shipped precision, not recall. |
| **P5** | PASS | Mutation-proven both directions: real English rows through the live PR path capture nothing, and a real English document is unaffected by the PR rules being registered. |

QA cycle 6 confirmed no gate moved on the strength of the bug fixes except
P2's precision caveat — correctly, since precision fixes must not move a
recall-gated verdict. Capture is unchanged by a corpus-exhaustive invariant:
neither fix adds or removes a row from either extractor's non-empty-candidate
set anywhere in 23,636 rows.

**Also settled:** 6/6 fixtures byte-exact (re-verified by the manager, not
taken from QA); of the 6 declared-vacuous greens, 5 became genuinely
meaningful post-registration and 2 remain vacuous for named structural
reasons (mutation-proven); counter audit closed with caveat (below).

## M-R14 — ENTRY CRITERIA for the held work (blocking, not advisory)

Before item 18c or ANY canonical-path held item (19-24) ships once core's
dispatch merges, both must be satisfied — these are gates on resuming, so
they cannot be lost in the wake excitement:

1. **`_UNQUOTED_TERM_DASH_RE` is fixed or narrowed FIRST.** QA's per-block
   measurement (the correct denominator; my own whole-body one was
   directionally uninformative) found **235 changed outcomes at ~30-35%
   precision**. Harmless today only because
   `pr_profile.extract_definitions_from_section` has zero production
   callers — it goes live the moment dispatch lands.
2. **18c's by-construction claim is RE-MEASURED empirically** against the
   dispatched pipeline — the full 117/633 canonical-leak measurement, not
   the single-row invariance test, which its own docstring says does not
   discharge this duty. Never assumed.
3. **`extract_local_definitions`'s docstring states a FALSE invariant** —
   it claims the footer strip leaves "match positions … unchanged"; measured,
   positions shift 142/160 chars (`STATE_PR_LEY_236_2015_ART12`,
   `STATE_PR_LEY_83_1941_SEC28`). Harmless only while no offset escapes that
   function (verified: it derives none, and `DefinitionCandidate` has no
   positional field). **Correct the comment before ANY offset-dependent
   logic is added there** — stamping a `unit_path` from a match offset is
   exactly what the post-dispatch canonical work invites.

**Counter audit (closed, with caveat):** QA reconstructed the inherited
`dev_complete_items: 10` as items 1,2,4,10-16 — arithmetic-consistent but
NOT directly recorded (`## Dev Complete` read `_None._` when the counter
said 10). Provenance unrecoverable; `14` stands. Closed, no further
archaeology.

## Dev Complete

- **18a + 26** — 2 `ScopeTriggerRule`s registered (`us_pr_scope_triggers.py`);
  `extract_local_definitions` widened (QA cycle-4 findings 1-4; finding 5
  correctly left strict-xfail). Manager-verified `0ed731e`.
- **27** — `CitationRule` registered (`us_pr_citations.py`), L.P.R.A.
  baseline-ordering limitation pinned as accepted.
- **28** — P3 article-scope, both directions, live-path.
- **29** — P5 two-sided language regression, live-path.

## Completed

_None._

## Context Dump

**PARKED, clean, awaiting the core DISPATCH merge.** Suite `30 failed / 915
passed / 12 xfailed` — the 30 are the held partition (byte-verified), the 12
are `xfail(strict, raises=)` deferrals. Gate table above is QA-settled.
**ON WAKE, M-R14's THREE entry criteria are BLOCKING — satisfy BEFORE any
canonical-path item (18c, 19-24) ships:** (1) fix/narrow
`_UNQUOTED_TERM_DASH_RE` (235 changed outcomes, ~30-35% precision; dead code
only until dispatch lands); (2) RE-MEASURE 18c's by-construction claim on the
full 117/633 canonical population, never assume it; (3) correct the false
"positions unchanged" docstring before adding offset-dependent logic there.
